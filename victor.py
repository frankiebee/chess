class Pawn(object):
	def __init__(self,start,type,player):
		self.start = start
		self.type = type
		self.player = player
		
	def possible(self):
		x = start[0]
		y = start[1]
		pos = [] 
		
		[x,y]
		
		[x,y+1]
		[x,y+2]
		[x+1,y+1]
		if x > 1:
		
		[x-1,y+1]
			
	
	
	
class Board(object):
	def __init__(self,x):
		self.x = x.lower()
		self.trans = {} # translater
		self.board = []
		
		for x in range(1,9)
			for y in range(1,9)
				
				self.board.append([x,y])				






